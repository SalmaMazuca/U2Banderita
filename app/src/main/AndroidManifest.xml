<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.edu.itl.c17130801.u2banderitaapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.U2BanderitaApp">
        <activity android:name=".RojoActivity"></activity>
        <activity android:name=".BlancoActivity" />
        <activity android:name=".VerdeActivity" />
        <activity android:name=".BanderaTresLayoutsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BanderaCompletaActivity" />
        <activity android:name=".AcercaDeActivity"/>
    </application>

</manifest>